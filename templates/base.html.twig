<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    {#
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    #}
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="
    Services to dar est une application web et mobile créée pour
        faciliter l'accès aux services de déménageurs et des artisans
        qu'on a besoin dans notre vie quotidienne et nos événements à
        travers une méthode moderne qui consiste à créer un réseau
        social professionnel reliant tous les prestataires de secteur de
        services et les clients par une plateforme en ligne.
    ">
    <meta name="keywords" content="Fournisseur,Produit,Service,Tunisie,Commander,Démanagement,Artisans,Service to dar,Maison">
    <!--Header always set X-Frame-Options "SAMEORIGIN" -->
    <title>{% block title %}ServiceToDar{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle` and uncomment the
    following Encore helpers to start using Symfony UX #} {% block stylesheets
    %} {#{{
      encore_entry_link_tags("app")
    }}#}

        {{
        encore_entry_link_tags("app")
        }}
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css" />
        <link
                rel="stylesheet"
                href="https://fonts.googleapis.com/css?family=Lato"
        />
        <link
                rel="stylesheet"
                href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
        />
        <link
                rel="stylesheet"
                href="https://www.w3schools.com/lib/w3-colors-2021.css"
        />

        <link
                rel="stylesheet"
                href="{{ asset('bundles/mercuryseriesflashy/css/flashy.css') }}"
        />
        <link
                href="//fonts.googleapis.com/icon?family=Material+Icons"
                rel="stylesheet"
        />
        <link
                href="//fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600,700"
                rel="stylesheet"
        />
        <style>
            * {
                font-family: Arial, Helvetica, sans-serif;
            }
        </style>

        {#
        <link href="/your-path-to-fontawesome/css/all.css" rel="stylesheet" />
        #} {% endblock %} {% block javascripts %} {#{{
      encore_entry_script_tags("app")
    }}#}
        {{
        encore_entry_script_tags("app")
        }}
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="//code.jquery.com/jquery.js"></script>
        <script src="{{
        asset('bundles/mercuryseriesflashy/js/flashy.js')
        }}"></script>
        <script
                src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.2.0/chart.min.js"
                integrity="sha512-VMsZqo0ar06BMtg0tPsdgRADvl0kDHpTbugCBBrL55KmucH6hP9zWdLIWY//OTfMnzz6xWQRxQqsUFefwHuHyg=="
                crossorigin="anonymous"
        ></script>
    {% endblock %}
    <style>
        *{
            font-family: calibre;
        }
        .container-fluid nav {
            background-color: #2c334e; /*#f0b943;*/ /* #9586db;*/
            color: white;
        }
        .container-fluid nav ul {
            background-color: #2c334e; /* #9586db;*/
            color: white;
        }
        .container-fluid nav ul li {
            background-color: #2c334e; /* #9586db;*/
            color: white;
        }
        .container-fluid nav ul li a:hover {
            color: grey;
        }
        .container-fluid nav ul li a {
            color: white;
        }
        .container-fluid nav ul li a i {
            color: white;
        }
        footer{
            background-color: #1a2c2d;
        }
        footer a{
            color: white;
        }
        #navb{
            background-color: #2c334e; /*#f0b943;*/ /* #9586db;*/
            font-family: calibre;
            font-size: large;
            font-weight: bold;
            color: white;
            width: 100%;
        }

        .n{
            margin-left: 450px;
        }
        .nn{
            margin-left: 25px;
        }
        .logo{
            margin-left: 70px;
        }
        @media only screen and (max-width: 480px) {
            .n{
                margin-left: 0px;
            }
            .nn{
                margin-left: 0px;
            }
            .logo{
                margin-left: 0px;
            }
        }
        label{
            font-weight: bold;
        }
    </style>
</head>
<body class="bg-light">
<div class="container-fluid">
    <nav class="navbar navbar-expand-lg fixed-top" id="navb">
        {# <div class="container-fluid">
          <a class="navbar-brand" href="#">
            <img
              src="{{ asset('/img/logo std.png') }}"
              alt="LOGO"
              width="40"
              height="40"
              style="background-color: none;"
          />
          </a>
        </div> #}
        <div class="container-fluid">
            <a class="navbar-brand logo" href="#" >
                <img
                        src="{{ asset('/img/logo std.png') }}"
                        alt="LOGO"

                        style="background-color: none;width:100px;height:60px;"
                />
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation" style="color: white;">
                <span><i class="fas fa-bars" style="color: white;"></i></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarText">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active n" aria-current="page"  href="{{ path('app_accueil') }}#accueil">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active nn" aria-current="page"  href="{{ path('app_accueil') }}#categories">Catégories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active nn " aria-current="page"  href="{{ path('app_accueil') }}#contact">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active nn " aria-current="page"  href="{{ path('app_chercher_fournisseurs') }}">Recherche</a>
                    </li>
                    {% if app.user != null %}
                        {% if is_granted('ROLE_CLIENT') %}
                            <li class="nav-item dropdown" >
                                <a
                                        href="#"
                                        class="nav-link dropdown-toggle nn"
                                        id="navbarDropdown"
                                        role="button"
                                        data-bs-toggle="dropdown"
                                        aria-expanded="false"
                                ><img
                                            src="{{asset('/img/photo_profil_utilisateurs/'~ app.user.photo)}}"
                                            alt="LOGO"
                                            class="img-fluid"
                                            style="border-radius: 50%;border:4px solid white;width:50px;height:50px;margin-left:20px;"

                                    /></a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_profil') }}">Voir Profil</a>
                                    </li>
                                    <li class="navigate">
                                        <a class="dropdown-item"href="{{ path('app_update_client',{user:app.user.id}) }}">Modifier Profil</a>
                                    </li>
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_consulter_favoris',{user:app.user.id}) }}">Voir favoris</a>
                                    </li>
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_mes_conversations',{user:app.user.id}) }}">Voir conversations</a>
                                    </li>
                                    <li class="navigate">
                                        <a href="{{ path('app_consulter_commandes',{user:app.user.id}) }}" class="dropdown-item"
                                        >Voir commandes</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_logout') }}"
                                        >Se déconnecter</a
                                        >
                                    </li>
                                </ul>
                            </li>

                        {% endif %}
                        {% if is_granted('ROLE_FOURNISSEUR') %}
                            <li class="nav-item dropdown">
                                <a
                                        href="#"
                                        class="nav-link dropdown-toggle"
                                        id="navbarDropdown"
                                        role="button"
                                        data-bs-toggle="dropdown"
                                        aria-expanded="false"
                                ><img
                                            src="{{asset('/img/photo_profil_utilisateurs/'~ app.user.photo)}}"
                                            alt="LOGO"
                                            class="img-fluid"
                                            style="border-radius: 50%;border:4px solid white;width:50px;height:50px;"
                                    /></a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_profil') }}"
                                        >Voir profil</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_update_fournisseur',{user:app.user.id}) }}"
                                        >Modifier profil</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_fournisseur_produit',{user:app.user.id}) }}"
                                        >Voir mes produits</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_service_fournisseur',{user:app.user.id}) }}"
                                        >Voir mes services</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_mes_conversations',{user:app.user.id}) }}"
                                        >Voir conversations</a
                                        >
                                    </li>

                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_commande_acceptées',{user:app.user.id}) }}"
                                        >Commande acceptées</a
                                        >
                                    </li>
                                    {% if app.user.isActive %}
                                        <li class="navigate">
                                            <a
                                                    class="dropdown-item"
                                                    href="{{ path('app_user_desactivate_account',{user:app.user.id}) }}"
                                            >Désactiver compte</a
                                            >
                                        </li>
                                    {% else %}
                                        <li class="navigate">
                                            <a
                                                    class="dropdown-item"
                                                    href="{{ path('app_user_activate_account',{user:app.user.id}) }}"
                                            >Activer compte</a
                                            >
                                        </li>

                                    {% endif %} {% if res is defined %} {% if res < 11 %}



                                        <li class="navigate">
                                            <a class="dropdown-item" href="{{ path('app_paiement') }}"
                                            >Renouveler abonnement</a
                                            >
                                        </li>
                                    {% else %} {% endif %} {% else %}

                                    {% endif %}
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_logout') }}"
                                        >Se déconnecter</a
                                        >
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item dropdown">
                                <a
                                        href="#"
                                        class="nav-link dropdown-toggle"
                                        id="navbarDropdown"
                                        role="button"
                                        data-bs-toggle="dropdown"
                                        aria-expanded="false"
                                ><img
                                            src="{{asset('/img/photo_profil_utilisateurs/'~ app.user.photo)}}"
                                            alt="LOGO"
                                            class="img-fluid"
                                            style="border-radius: 50%;border:4px solid white;width:50px;height:50px;"
                                    /></a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_profil') }}"
                                        >Voir profil</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_update_admin',{user:app.user.id}) }}"
                                        >Modifier profil</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_admin_utilisateurs') }}"
                                        >Voir utilisateurs</a
                                        >
                                        {# app_categories_admin #}
                                    </li>

                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_categories_admin') }}"
                                        >Voir catégories</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a
                                                class="dropdown-item"
                                                href="{{ path('app_statistique') }}"
                                        >Générer statistiques</a
                                        >
                                    </li>
                                    <li class="navigate">
                                        <a class="dropdown-item" href="{{ path('app_logout') }}"
                                        >Se déconnecter</a
                                        >
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link active nn" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                    {% endif %}


                </ul>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 " >
                    <!-- {% if app.user != null %}
              {% if is_granted('ROLE_CLIENT') %}
              <li class="nav-item dropdown" >
                <a
                  href="#"
                  class="nav-link dropdown-toggle"
                  id="navbarDropdown"
                  role="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                  ><img
                    src="{{asset('/img/photo_profil_utilisateurs/'~ app.user.photo)}}"
                    alt="LOGO"
                    class="img-fluid"
                    style="border-radius: 50%;border:4px solid white;width:50px;height:50px;"

                /></a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_profil') }}">Voir Profil</a>
                  </li>
                  <li class="navigate">
                    <a class="dropdown-item"href="{{ path('app_update_client',{user:app.user.id}) }}">Modifier Profil</a>
                  </li>
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_consulter_favoris',{user:app.user.id}) }}">Voir favoris</a>
                  </li>
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_mes_conversations',{user:app.user.id}) }}">Voir conversations</a>
                  </li>
                  <li class="navigate">
                    <a href="{{ path('app_consulter_commandes',{user:app.user.id}) }}" class="dropdown-item"
                      >Voir commandes</a
                    >
                  </li>
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_logout') }}"
                      >Se déconnecter</a
                    >
                  </li>
                </ul>
              </li>

              {% endif %}
              {% if is_granted('ROLE_FOURNISSEUR') %}
              <li class="nav-item dropdown">
                <a
                  href="#"
                  class="nav-link dropdown-toggle"
                  id="navbarDropdown"
                  role="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                  ><img
                    src="{{asset('/img/photo_profil_utilisateurs/'~ app.user.photo)}}"
                    alt="LOGO"
                    class="img-fluid"
                    style="border-radius: 50%;border:4px solid white;width:50px;height:50px;"
                /></a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_profil') }}"
                      >Voir profil</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_update_fournisseur',{user:app.user.id}) }}"
                      >Modifier profil</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_fournisseur_produit',{user:app.user.id}) }}"
                      >Voir mes produits</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_service_fournisseur',{user:app.user.id}) }}"
                      >Voir mes services</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_mes_conversations',{user:app.user.id}) }}"
                      >Voir conversations</a
                    >
                  </li>

                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_commande_acceptées',{user:app.user.id}) }}"
                      >Commande acceptées</a
                    >
                  </li>
                  {% if app.user.isActive %}
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_user_desactivate_account',{user:app.user.id}) }}"
                      >Désactiver compte</a
                    >
                  </li>
                  {% else %}
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_user_activate_account',{user:app.user.id}) }}"
                      >Activer compte</a
                    >
                  </li>

                  {% endif %} {% if res is defined %} {% if res < 11 %}



                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_paiement') }}"
                      >Renouveler abonnement</a
                    >
                  </li>
                  {% else %} {% endif %} {% else %}

                  {% endif %}
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_logout') }}"
                      >Se déconnecter</a
                    >
                  </li>
                </ul>
              </li>
              {% endif %}
              {% if is_granted('ROLE_ADMIN') %}
              <li class="nav-item dropdown">
                <a
                  href="#"
                  class="nav-link dropdown-toggle"
                  id="navbarDropdown"
                  role="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                  ><img
                    src="{{asset('/img/photo_profil_utilisateurs/'~ app.user.photo)}}"
                    alt="LOGO"
                    class="img-fluid"
                    style="border-radius: 50%;border:4px solid white;width:50px;height:50px;"
                /></a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_profil') }}"
                      >Voir profil</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_update_admin',{user:app.user.id}) }}"
                      >Modifier profil</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_admin_utilisateurs') }}"
                      >Voir utilisateurs</a
                    >
                    {# app_categories_admin #}
                  </li>

                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_categories_admin') }}"
                      >Voir catégories</a
                    >
                  </li>
                  <li class="navigate">
                    <a
                      class="dropdown-item"
                      href="{{ path('app_statistique') }}"
                      >Générer statistiques</a
                    >
                  </li>
                  <li class="navigate">
                    <a class="dropdown-item" href="{{ path('app_logout') }}"
                      >Se déconnecter</a
                    >
                  </li>
                </ul>
              </li>
              {% endif %}
              {% else %}
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
              </li>
              {% endif %} -->
                </ul>
            </div>
    </nav>
</div>
{% block body %}{% endblock %}

{{ include("@MercurySeriesFlashy/flashy.html.twig") }}
</body>
</html>
